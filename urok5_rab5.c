#include<stdio.h>

int count_brackets(char brackets){

}

int main(){
    char characters;
    do{
        characters = getchar();
        putchar(characters);
    }while (characters != '.');
    //for(int i =0; i<)
    //printf("\n%d", sizeof(characters)/sizeof(characters));
}
